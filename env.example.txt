# Carfax Scraper Configuration
# ============================
# انسخ هذا الملف إلى .env واملأ البيانات

# بيانات الاعتماد (مطلوبة للتسجيل التلقائي)
CARFAX_EMAIL=your_email@example.com
CARFAX_PASSWORD=your_password

# إعدادات عامة
HEADLESS=true
OUTPUT_DIR=data/output
COOKIES_FILE=data/cookies.txt
TOKENS_FILE=data/tokens.json

# تأخير بين الطلبات (ثواني)
MIN_DELAY=2
MAX_DELAY=5

# ============================
# Bright Data Proxy (اختياري)
# ============================
# قم بتفعيل البروكسي لتجنب الحظر عند السحب بكميات كبيرة

# تفعيل/تعطيل البروكسي
PROXY_ENABLED=false

# عنوان سيرفر Bright Data
PROXY_SERVER=brd.superproxy.io:33335

# بيانات الاعتماد (من لوحة تحكم Bright Data)
# الصيغة: brd-customer-CUSTOMER_ID-zone-ZONE_NAME
PROXY_USERNAME=brd-customer-hl_XXXXX-zone-XXXXX
PROXY_PASSWORD=your_proxy_password

# استهداف جغرافي (اختياري)
# us = الولايات المتحدة، uk = بريطانيا، إلخ
PROXY_COUNTRY=us

